

trigger: none

stages:
- stage: 
  jobs:
    - deployment: 
      environment:
       name: dev
       resourceType: virtualMachine
      strategy:
       runOnce:
         deploy:
           steps:
           
           - task: CmdLine@2
             inputs:
               script: 'sudo cp -r $(System.DefaultWorkingDirectory)/README.md /var/www/html/'