

trigger: none

stages:
- stage: 
  jobs:
    - deployment: 
      environment:
       name: qa
       resourceType: virtualMachine
      strategy:
       runOnce:
         deploy:
           steps:
           
           - task: CmdLine@2
             inputs:
               script: 'cp -r $(System.DefaultWorkingDirectory)/README.md /var/www/html/'